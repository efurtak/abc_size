# frozen_string_literal: true

RSpec.describe Abcsize do
  it "has a version number" do
    expect(Abcsize::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
